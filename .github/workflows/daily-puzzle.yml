name: Daily Crossword Build

on:
  schedule:
    - cron: '5 0 * * *'
  workflow_dispatch:

jobs:
  build-puzzle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate today's puzzle
        id: gen
        run: |
          cat > puzzle.json <<'EOF'
          {
            "date": "${{ github.event.schedule || 'manual' }}",
            "width": 5,
            "height": 5,
            "cells": [
              1,0,0,0,0,
              null,null,null,null,null,
              2,0,0,0,0,
              null,null,null,null,null,
              3,0,0,0,0
            ],
            "solution": [
              "H","E","L","L","O",
              "","","","","",
              "W","O","R","L","D",
              "","","","","",
              "L","I","V","E","S"
            ],
            "across": [
              { "num": 1, "clue": "Greeting" },
              { "num": 2, "clue": "Planet we live on" },
              { "num": 3, "clue": "Plural of life" }
            ],
            "down": []
          }
          EOF

      - name: Commit puzzle.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add puzzle.json
          git commit -m "Update puzzle for ${{ github.event.schedule || 'manual' }}"
          git push